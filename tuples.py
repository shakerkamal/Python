# Tuples are immutable lists.
# Use tuples when you want to define a list that cannot change.

rgb = ('red', 'green', 'blue')

print(rgb[0])
print(rgb[1])
print(rgb[2])


numbers = (3,)  # to define tuple with one element you need a trailing comma
print(type(numbers))